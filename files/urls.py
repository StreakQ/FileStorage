from django.urls import path
from . import views

app_name = 'files'

urlpatterns = [
    # path('', home_view, name='home'),
    # path('upload/', upload_file_view, name='upload'),
    # path('search/', search_view, name='search'),
    # path('delete/<str:file_path>/', delete_file_view, name='delete_file'),
    # path('rename/<str:file_path>/', rename_file_view, name='rename_file'),
]